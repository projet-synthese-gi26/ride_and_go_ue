name: Spring Boot CI/CD

on:
  push:
    branches:
      - "**"
jobs:
  pipeline:
    uses: projet-synthese-gi26/workflows/.github/workflows/spring-boot-ci.yml@main
    with:
      app_name: auth-service
    secrets: inherit          # passe TOUS les secrets automatiquement
